// Placeholder TU so the `tea` static library target is non-empty during Phase 0.
// Real sources are added in Phase 1+. Remove this file when the library has
// any other .cpp file.

namespace tea {
inline void _tea_marker() {}
}  // namespace tea
